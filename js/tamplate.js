const person = 'Ab Naeem';
const person2 = 'Md Hafiz';
const person3 = `Pranta Ray`; // Backtik - ``

const multiLine = `First Line text
Second line text
Third line text
Fourth line text`;
console.log(multiLine);

// Dynamic value pass by Template String (Backtic - ``)
const a = 20;
const b = 30;
const sum = `Sum of ${a} and ${b} is: ${a + b}`;
console.log(sum);
